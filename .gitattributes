# Normalise to LF in the repository and in every working copy.
# The containers build on Linux, so CRLF must never reach a Dockerfile,
# an Nginx config or a shell script.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
